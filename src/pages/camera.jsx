import { memo } from "react";
import { View, Text } from "react-native";

const Home = memo(()=>{
    return (
        <View>
            <Text>
                1
            </Text>
        </View>
    )
})

export default Home